# Copilot instructions for this workspace

- This is a React + TypeScript app scaffolded with Vite.
- Prefer small, focused changes that fit the existing Vite structure.
- Use the provided npm scripts: `dev`, `build`, `lint`, and `preview`.
- Keep the codebase clean and avoid adding extra dependencies unless needed.
- When editing UI, keep the styling consistent and avoid unnecessary complexity.
